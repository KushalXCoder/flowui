---
title: Alert
description: Displays a callout for user attention.
---

import { Terminal, AlertCircle } from 'lucide-react';
import { Button } from '@/components/ui/button';

<Callout title="Notice" type="warn" icon={<AlertCircle className="w-4 h-4" />}>
    The Alert component is a great way to communicate important information.
</Callout>

## Usage

<Tabs items={['Preview', 'Code']}>
  <Tab value="Preview" className="p-8 border rounded-lg flex items-center justify-center min-h-[120px] bg-zinc-50 dark:bg-zinc-900/50">
    <div className="rounded-lg border bg-background p-4 flex gap-4 w-full max-w-md shadow-sm">
      <Terminal className="h-4 w-4 mt-1" />
      <div>
        <h5 className="font-medium leading-none tracking-tight mb-1">Heads up!</h5>
        <div className="text-sm text-muted-foreground">
            You can add components to your app using the cli.
        </div>
      </div>
    </div>
  </Tab>
  <Tab value="Code">
    ```tsx
    import { Alert, AlertDescription, AlertTitle } from "@/components/ui/alert"
    import { Terminal } from "lucide-react"
 
    export function AlertDemo() {
      return (
        <Alert>
          <Terminal className="h-4 W-4" />
          <AlertTitle>Heads up!</AlertTitle>
          <AlertDescription>
            You can add components to your app using the cli.
          </AlertDescription>
        </Alert>
      )
    }
    ```
  </Tab>
</Tabs>

## Examples

### Destructive

<div className="border rounded-lg p-6 bg-red-50/10 border-red-200/20">
    <div className="rounded-lg border border-destructive/50 text-destructive dark:border-destructive [&>svg]:text-destructive bg-destructive/10 p-4 flex gap-4 w-full items-start">
        <AlertCircle className="h-4 w-4 mt-1" />
        <div>
            <h5 className="font-medium leading-none tracking-tight mb-1">Error</h5>
            <div className="text-sm opacity-90">
            Your session has expired. Please log in again.
            </div>
        </div>
    </div>
</div>
